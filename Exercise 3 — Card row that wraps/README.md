# Exercise 3 — Card row that wraps Make 8 cards in a container:

Cards should form rows

Each card has a title + short text

Layout should wrap nicely

Each card should be at least 220px wide

Hint: flex: 1 1 220px + flex-wrap: wrap + gap.
