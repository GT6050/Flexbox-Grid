# Exercise 1 — Flexbox Centering

Make a .box centered both horizontally and vertically on the page.

Use display: flex

Use min-height: 100vh

Use place-content? (No—Flex doesn’t have it. Use justify-content + align-items.)

Success check: Resize window, it stays dead center.
